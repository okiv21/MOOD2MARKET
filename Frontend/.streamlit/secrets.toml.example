# Copy this file to ".streamlit/secrets.toml" and fill in your key.
# The real secrets.toml is git-ignored and must never be committed.
# On Streamlit Community Cloud, set this under App settings > Secrets instead.

GROQ_API_KEY = "your_groq_api_key"
